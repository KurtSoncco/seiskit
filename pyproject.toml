[project]
name = "seiskit"
version = "0.1.0"
description = "This project is for OpenSees study on spatial variability in soil wave propagation."
authors = [
    { name = "Kurt Walter Soncco Sinchi", email = "kurtwal98@berkeley.edu" },
]
# Narrows the compatible Python versions to prevent future errors
requires-python = ">=3.10" 
dependencies = [
    "numpy",
    "scipy",
    "openseespy",
    "joblib",
    "numba",
    "matplotlib",
    "seaborn",
    "plotly",
]

[project.optional-dependencies]
analysis = [
    "pandas",
    "h5py",
    "hdf5plugin",
    "tqdm",
    "statsmodels",
    "kerchunk",
    "zarr",
    "xarray",
    "fsspec",
    "ujson",
]
ml = [
    "scikit-learn",
    "xgboost",
    "shap",
    "interpret",
    "pygam",
    "bambi",
    "arviz",
]
notebook = [
    "ipykernel",
    "kaleido",
]
dev = [
    "ruff",
    "pytest",
    "pytest-cov",
]

[tool.setuptools]
packages = ["seiskit"]

[tool.pytest.ini_options]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [".venv", "build", "dist", "*.egg-info"]

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]  # line length; can tighten later


[tool.uv]
package = true

[dependency-groups]
dev = [
    "pytest-cov>=7.0.0",
]

[project.urls]
"Homepage" = "https://example.invalid/"

